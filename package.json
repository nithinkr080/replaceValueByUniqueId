{
  "name": "replacevaluebyuniqueid",
  "version": "1.0.8",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc --build --watch",
    "build": "rm -rf dist && npx tsc --build",
    "prepublishOnly": "pnpm run build"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.11.30"
  },
  "author": "Nithin K R",
  "files": [
    "dist"
  ]
}
